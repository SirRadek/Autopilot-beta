# F0.5 — Ghost-pattern taxonomy proposal (owner sign-off gate)

**Status:** PROPOSAL — pending owner decision. No schema change (F1) may start until the
`Owner decision` column is filled and signed off (plan §F0.5 gate).
**Author:** Opus (decision analyst). Creative opponent lane (Gemini/`agy`) = MISSING
(WORKER-CLI-001) → this is Opus-only analysis pending owner judgment.
**Base:** autopilot@599785fb · **Source:** F0 inventory (`PDOS_GHOST_PATTERN` ×13 / 12 distinct).

## What this gate decides

The 13 ghost occurrences are recipe `allowed_patterns` ids absent from `pattern-manifest`.
Each must be classed as **pattern** (a section/UI block in the manifest taxonomy:
conversion/layout/motion/ux/data/logic_pattern), **direction** (an art/theme direction —
NOT currently a first-class type; would be introduced by F1 `direction_ids`), or **asset**
(a concrete reusable asset: mascot, motion effect, background).

**Two structural decisions drive F1's schema and must be settled first:**

- **D1 — Is "direction" a first-class type?** `theme-crossed-direction` already exists as a
  `layout_pattern`. The ghosts `theme-crossed-motion` and `theme-crossed-positioning` are its
  motion/positioning treatments. Either (a) introduce a real `direction` concept (F1 adds
  `direction_ids`), or (b) collapse them into the existing pattern with modifiers (no new type).
- **D2 — Split asset from pattern?** `cat-concierge-guide` conflates a specific mascot *asset*
  ("cat concierge") with a *pattern* (guided mascot, ≈ existing `mascot-progressive-guide`).
  Either split into `asset_id` + `pattern_id`, or keep as one pattern.

## Domain A — recipe `creative-motion` (7 ghosts)

| # | ghost value | proposed category | anchor / rationale | risk | migration impact | Owner decision |
|---|---|---|---|---|---|---|
| A1 | `animated-hero` | **pattern** (motion_pattern) | motion variant of `sharp-positioning-hero` | low | new manifest entry; recipe ref unchanged | _PENDING_ |
| A2 | `theme-crossed-motion` | **direction** (D1) or alias→`theme-crossed-direction` | motion treatment of existing direction-pattern | med (duplication) | new `direction_id` OR alias | _PENDING_ |
| A3 | `cat-concierge-guide` | **asset + pattern** (D2) | "cat concierge" mascot asset + guide behavior (≈`mascot-progressive-guide`) | med (conflation) | split `asset_id`+`pattern_id` OR single pattern | _PENDING_ |
| A4 | `section-aware-mascot` | **pattern** or alias→`mascot-progressive-guide` | near-duplicate of existing mascot pattern | med (duplication) | new pattern OR alias | _PENDING_ |
| A5 | `demo-world-hub` | **pattern** (ux_pattern) | interactive demo-hub section; **shared with marketing-premium** | med (shared → must be consistent) | single manifest entry, both recipes ref it | _PENDING_ |
| A6 | `mask-reveal` | **asset** (motion effect) | reveal technique, not a section | low | new `asset_id` (motion) | _PENDING_ |
| A7 | `motion-background` | **asset** (background) | animated background; `assets/backgrounds/` already holds theme svgs | low | new `asset_id` (background) | _PENDING_ |

## Domain B — recipe `marketing-premium` (6 ghosts; `demo-world-hub` = same as A5)

| # | ghost value | proposed category | anchor / rationale | risk | migration impact | Owner decision |
|---|---|---|---|---|---|---|
| B1 | `theme-crossed-positioning` | **direction** (D1) or alias→`theme-crossed-direction` | positioning treatment of existing direction-pattern | med (duplication) | new `direction_id` OR alias | _PENDING_ |
| B2 | `guided-offer-map` | **pattern** (ux/conversion) | offer-navigation section | low | new manifest entry | _PENDING_ |
| B3 | `studio-proof-ledger` | **pattern** (conversion) | proof section; overlaps `proof-led-section` | low-med (overlap) | new entry OR alias | _PENDING_ |
| B4 | `case-study-strip` | **pattern** (conversion) | case-study section (≈`scroll-linked-proof`/`proof-led-section`) | low | new manifest entry | _PENDING_ |
| B5 | `outcome-cta` | **pattern** (conversion) | outcome-focused CTA section | low | new manifest entry | _PENDING_ |
| B6 | `demo-world-hub` (dup) | = A5 | dedupe: one shared entry | — | recipe ref points at the single A5 entry | _PENDING_ |

## Opus recommendation (for owner to accept / override)

- **Clear patterns (batch-approve):** A1, A4, A5/B6, B2, B3, B4, B5 → adopt as manifest patterns
  (A4 likely alias to `mascot-progressive-guide`; B3 verify vs `proof-led-section` to avoid overlap).
- **D1:** lean **(b) collapse** `theme-crossed-{motion,positioning}` into `theme-crossed-direction`
  with a modifier — avoids a new type until there's a second consumer. Revisit if directions
  proliferate. (Owner may prefer (a) if "direction" is strategically first-class.)
- **D2:** lean **split** `cat-concierge-guide` → asset `cat-concierge` + pattern
  `mascot-progressive-guide` — keeps the mascot character reusable and the pattern generic.
- **Assets:** A6 `mask-reveal`, A7 `motion-background` → assets, not patterns.

## After sign-off

Filled decisions feed **F1** (dual-read schema: `allowed_pattern_ids` / `direction_ids` /
`required_sections`; asset ref/tag split) and **F2** (data migration per domain). Until then,
F1 stays blocked.
