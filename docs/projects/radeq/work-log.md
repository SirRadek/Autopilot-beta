# Radeq — work log (local stub)

This file is a local placeholder only. The canonical RadeQ work log lives in
the sibling `SirRadek/radeq` repository, at `.autopilot/work-log.md`
(`kind: external_reference`, `authority: project-source` — see
`prompt-library/source-catalog.json` id `radeq-project-work-log`). Do not treat
entries added here as the source of truth for RadeQ project history; update
the log in `SirRadek/radeq` instead and reference it from here if a local
pointer is needed.

## Log

- 2026-07-03: Bootstrap entry. This stub was created to give
  `docs/projects/radeq/` a resolvable local `work-log.md` reference. No
  RadeQ project history is recorded here; see the canonical log in
  `SirRadek/radeq:.autopilot/work-log.md`.
