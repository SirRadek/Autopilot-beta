# Nonsecret provider-probe policy for /etc/autopilot/control-plane-probes.env.
# Accepted comma-separated names are codex, claude, and agy. Unknown names
# or empty comma segments prevent the control plane from starting.
CONTROL_PLANE_USAGE_PROBES=
