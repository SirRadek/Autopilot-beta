node_modules/
cockpit/node_modules/
cockpit/dist/
*.log
.DS_Store
# Beta keeps evidence/scratch out of git (no raw logs/secrets committed).
output/
state/
radeq_tmp/
*.stackdump
.codex/state/
docs/autopilot/session-state/
.claude/

# OpenRouter attempts budget counter — runtime state, never committed
docs/autopilot/openrouter-api-attempts.jsonl
# OpenRouter spend ledger — runtime state, never committed
docs/autopilot/openrouter-api-spend.jsonl
run-codex-*.sh
test-results/
cockpit/design/
